#include <stdio.h> 
#include <stdlib.h> 
int main() 
{ 
 int size = 1000, outstr[1000]; 
 int strlen = 0, posstrlnght = 0, negstrlen = 0, dig, rank = 1, negrank = 1; 
 double dbl =0; 
 char smbl; 
 char *inptstr = (char*)malloc(sizeof(char) * size); 
 while ((smbl = getchar()) != '\n') 
 { 
  inptstr[strlen] = smbl; 
  strlen++;
  if(smbl = ',')
    {
    smbl = '.';
    }
 } 
 dbl = atof (inptstr);



 printf("%f\n%d\n", dbl, strlen); 
}
